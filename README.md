# Game theory and Reinforcement learning
Note this project is in process

## build and run
If u use vs code (u shuld use vs code because otherwise i wont help u run...), clone the repo and press f5 for debugging. 

In order to run witout debugging u should press ctrl + shift + p and then type "run"  and choose 'tasks run task'.